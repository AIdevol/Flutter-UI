import 'package:flutter/material.dart';
import 'package:hexcolor/hexcolor.dart';

LinearGradient timerColor = LinearGradient(
  colors: [HexColor('3D8E70'), HexColor('205E44')],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);

LinearGradient stopWatchColor = LinearGradient(
  colors: [HexColor('483078'), HexColor('3C2865')],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);

LinearGradient timer1Color = LinearGradient(
  colors: [HexColor('181818'), HexColor('090909')],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
